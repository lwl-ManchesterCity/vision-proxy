---
name: vision-proxy
description: 当对话中出现图片且当前主模型无原生视觉能力时自动使用——包括用户粘贴或上传图片、消息含本地图片路径或图片 URL、出现 "Saved attachments:" 图片列表、用户说"看 / 分析 / 识别这张截图"、"按截图还原页面 / 组件"、前端页面截图排错或 UI 还原、报错截图、架构图、数据图表等场景。通过多模态子代理（路线 A）或外部视觉 API（路线 B，scripts/vision.js）将图片转为结构化文本描述并持久化到 vision-transcripts/ 目录；自动在回复开头标注转写状态（[已转写] / [未转写] / [转写失败] / [追问]），支持按需追问图片细节。
---

# vision-proxy

把"看图"从主模型（强代码、可能无视觉）的能力里解耦出来，包成一个会话级可重复调用的视觉通道。

核心设计：主对话模型**永远不换**，上下文不断裂；图片以**结构化文本**（不是散文）回流到主上下文；当主模型对细节存疑时，可**追问**（只问几十 token 的 Q&A，避免重发全量描述）。

完整结构化 schema、路线 A / B 的 prompt 模板、追问规则见 `references/schemas.md`；本文件只描述主流程、路线决策、状态标注、持久化与降级策略。

---

## 0. 原生视觉判定（用户声明制，禁止模型自评）

**实测教训（2026-09-08）**：纯文本端点**无法可靠自评视觉能力**。harness 接受图片且不报错时，模型会声称"能看见"并当场编造描述——LongCat 2.0 曾把一张 KPL 电竞海报绘声绘色地编成"VS Code Diff 编辑器"。因此本 skill **禁止**模型用任何自评方式（包括"Read 一下看返回来判断"）得出"我有原生视觉"的结论。

判定规则（按顺序）：

1. 环境变量 `VISION_NATIVE_VISION=1`（**用户显式声明**端点具备原生视觉）→ 在回复开头标 `[未转写] <image_path> → 原因：用户声明的原生视觉`，跳过本 skill，用主模型自身能力继续；
2. 否则 → **一律进入 §1 主流程**（默认路线 B）。不做任何运行时探测，不需要 Read 尝试。

**用户如何决定是否声明**（一次性人工验证）：让 agent 描述本 skill 自带的金丝雀图 `assets/canary.png`，用户肉眼核对描述与图片实际内容是否一致。完全一致才设置 `VISION_NATIVE_VISION=1`；说不出、说错、编造任何细节 → 不要设置。端点换模型后需重新验证。

> 本 skill 只为无视觉能力的主模型服务；但"是否有视觉"只能由人裁决，不能由模型自证。

---

## 1. 触发条件

本 skill 已被本平台的 skill 自动匹配机制加载（frontmatter `description` 覆盖了所有图片相关触发信号），无需用户输命令。

具体触发信号（用于核对当前上下文是否真的涉及图片）：

- 用户消息包含图片附件（粘贴 / 上传 / 拖拽）；
- 消息含本地图片绝对路径（含 `Desktop/`、`Downloads/`、`/tmp/`、`/Users/...` 等）；
- 消息含 `http://` / `https://` 的图片 URL；
- 出现 `Saved attachments:` 列表项含图片；
- 用户口头说："看 / 分析 / 识别这张截图"、"按截图还原页面 / 组件"、"这是不是 bug"等。

**不**触发：用户输入纯文字、没有图片或图片链接；图片是 emoji / ASCII art / 已是 base64 字符串嵌入在代码里的占位资源。

---

## 2. 主流程

### 2.0 原生视觉判定

按 §0 执行：仅当用户已设置 `VISION_NATIVE_VISION=1` 时跳过本 skill；否则直接进入主流程。

### 2.1 准备本地图片

把图片落盘为**绝对路径**：

- 用户提供的路径直接使用；
- `Saved attachments:` 列表里给定的路径直接使用；
- 仅是 URL：下载到 `<cwd>/vision-transcripts/tmp/<sha1>.<ext>`（用 `curl -sSL -o <path> <url>`），并把下载后的路径作为图片路径；
- 没有任何方式获得本地文件 → 告知用户"请先把图片存到本地磁盘再发给我"，**终止**本 skill，不做任何凭空描述。

### 2.2 缓存命中检查

计算图片的 SHA1（`shasum -a 1 <path>` 或 `sha1sum`），遍历 `<cwd>/vision-transcripts/*.md` 的 frontmatter：

- 命中且 `image_sha1` 与本图一致 → 直接复用该文件的 `## FIRST PASS` 段作为本轮转写结果；在回复开头标 `[已转写·缓存命中] <image_path> → <持久化文件路径>`。
- 未命中 → 继续 2.3。

> 缓存替代了会话内临时缓存，跨会话复用，更省成本且可审计。

### 2.3 首过描述（first pass）

按 §4 的路线决策选择通道；调用时把以下信息塞进 prompt（详见 `references/schemas.md` §4 / §5）：

- 图片绝对路径；
- 任务上下文摘要（2–3 句，说明本模型在做什么、希望视觉模型关注什么）；
- 完整结构化 schema（直接复用 `references/schemas.md` §1 + §2）；
- （多图时）图片顺序与关联说明。

视觉模型**必须**先输出 `image_type`，再按对应分型输出 `structure`。`uncertainties` 字段是必填项。

### 2.4 回流与消费

- 把视觉通道的 stdout（或子代理返回值）作为转写结果，**追加到主上下文**。保留图片引用位置，不做占位符替换。
- **禁止改写视觉通道的原始返回**——不总结、不润色、不"翻译"。主模型基于原始文本继续任务，**不要**要求视觉模型重发全量。
- **硬性反幻觉规则**：输出 `[已转写]` 之前必须真实执行视觉通道调用（Bash 实际运行脚本 / 实际 spawn 子代理）并拿到非空返回。**禁止**在未实际调用的情况下凭记忆或猜测构造转写内容（2026-09-08 LongCat 2.0 幻觉事件：模型把 KPL 海报编成 VS Code diff）。
- **禁止"推断式诊断"**：判断通道可用性的唯一方式是**实际执行** `vision.js` 并看退出码。禁止用 `echo $VISION_API_KEY` 等方式检查环境变量后就宣布"key 未配置"——key 通常配置在 `scripts/.env` 文件里，shell 环境变量看不到它（2026-09-08 实测：模型 echo 看到 UNSET 就报 `[转写失败]`，但 `.env` 里 key 实际存在，脚本从未被执行）。

### 2.5 追问循环（必选机制）

结构化抽取**一定有损**——`uncertainties` 非空、所需参数缺失、transcription 有歧义时，**必须追问**（不要猜测）：

- 追问只问具体问题（如"登录按钮的背景 hex 多少？"），不是"帮我再仔细看看"；
- 追问 prompt 模板见 `references/schemas.md` §6；
- 追问回答**追加**到原 `vision-transcripts/<file>.md` 的 `## FOLLOW-UPS` 区块（按 `Q1/A1` 编号），不新建文件；
- 主上下文只追加几十 token 的 Q&A，不重复全量描述。

### 2.6 降级策略

以下任务类型**文本代理不适合**，明确告知用户"建议直接切换到原生多模态模型"：

1. **像素级 UI 复刻**（精确间距 / 色值还原）——视觉通道的 hex / 尺寸是近似描述，不是像素采样；
2. **密集小字图片**（满屏代码截图 / 大表格截图）——OCR 准确率与上下文长度不可兼得；
3. **视觉审美判断**（"哪个配色更好看"）——结构化 schema 本身不覆盖。

---

## 3. 结果可见性与持久化（强制）

自动触发意味着转写发生在"幕后"，必须用**两条机制**保证用户始终知情、结果始终可查。

### 3.1 对话内状态标注（强制）

每次图片处理完毕（含追问），主模型的下一条回复**必须**以一行标记开头。**5 种变体**：

```
[已转写] xxx.png → vision-transcripts/2026-09-08-101530-xxx.md
[已转写·缓存命中] xxx.png → vision-transcripts/2026-09-08-101530-xxx.md
[未转写] xxx.png → 原因（原生视觉可用 / 与任务无关 / 通道不可用）
[转写失败] xxx.png → 已标记 UNREADABLE，原因摘要
[追问] xxx.png → 第 N 轮（已追加到 vision-transcripts/<file>.md）
```

- 一张图一行；用户据此一眼判断"它看没看我的图、看得对不对"。
- 标注必须出现在正文之前，不埋在段落里。
- 多图：每张一行。
- `[已转写]` 行必须附持久化文件相对路径；文件不存在说明 §3.2 被跳过，属流程违规。

### 3.2 本地文件持久化（强制）

- **目录**：`<cwd>/vision-transcripts/`（无工作区时用图片同目录下的 `vision-transcripts/`）。
- **命名**：`YYYY-MM-DD-HHMMSS-<图片原名 sanitized>.md`。
- **格式**：

```markdown
---
image_path: /abs/path/xxx.png
image_sha1: <SHA1 十六进制>
image_type: ui_mockup
vision_channel: A(子代理, <model>) | B(API, <model>)
created: 2026-09-08T10:15:30+08:00
---

## FIRST PASS
<首过结构化输出全文（直接是视觉模型 stdout）>

## FOLLOW-UPS
### Q1: 按钮背景 hex？
A1: ...
### Q2: ...
```

- 持久化文件同时充当**跨会话缓存**（见 2.2）。
- 建议用户把 `vision-transcripts/` 加入 `.gitignore`（可能含敏感截图转录：token、密码、内网地址）。

### 3.3 审计输出（强制，用户可核验"通道真的被调用了"）

每次转写后，回复中必须包含完整审计块，位于状态行之后、主模型自己的分析之前：

```
[已转写] xxx.png → vision-transcripts/2026-09-08-113000-xxx.md

<vision-proxy-audit>
通道: B (API, qwen-vl-max @ https://dashscope.aliyuncs.com/compatible-mode/v1)
命令: node <skill>/scripts/vision.js <image_path> --prompt-file <prompt_file_path>
退出码: 0
--- 视觉通道原始返回（逐字，禁止改写） ---
<stdout 全文，含完整 schema 各字段>
--- END ---
</vision-proxy-audit>

<此后才是主模型基于转写内容的分析>
```

- **原始返回逐字引用**：不总结、不节选、不润色；schema 字段（`image_type` / `transcription` / `structure` / `uncertainties` 等）必须原样可见。
- 参数部分如实填写：通道、模型、API base、实际执行的命令、退出码。prompt 文件内容不必全文贴出，但路径必须给（用户可自行打开核对）。
- 审计块是用户区分"真调了视觉 API"与"模型又在编造"的唯一依据，**省略审计块视同流程违规**。
- 追问轮次从简：`[追问]` 状态行 + 只附本轮 Q&A 原文，不必重复完整审计块。

---

## 4. 视觉通道路线决策

按顺序检查，选首个成立的路线：

```
1. 路线 A（子代理路由，默认）
   条件：本平台已验证"spawn 子代理读图"可行
        （参考本文件末尾"平台验证记录"）

2. 路线 B（外部视觉 API，兜底）
   条件：环境变量 VISION_API_KEY 已配置（或当前目录 .env）
   脚本：scripts/vision.js（详见 references/schemas.md §5）

3. 都不可用
   行为：在回复开头标 [转写失败] xxx.png → 原因摘要，
        提示用户配置 VISION_API_KEY 后重试，或切换到原生多模态主模型。
```

**当前默认（2026-09-08 更新）**：目标部署环境为 **Claude Agent 框架 + 双 key**（coding 模型单模态，视觉模型独立 key）。该形态下子代理继承框架的 key 与模型，同样无视觉——**路线 A 默认不可用，默认走路线 B**（需配置 `VISION_API_KEY`）。

**路线 A 的唯一例外**：若 coding key 背后是聚合网关（一个 key 可调多个模型）且框架支持子代理级 model 覆盖，可将子代理模型指到网关上的多模态模型后启用路线 A。启用前必须按 §7 格式重新做验证记录；未验证前一律走 B。

---

## 5. 多图 / 失败兜底 / 隐私

### 5.1 多图

- **前后关联的多张图**（操作步骤序列、报错前后对比）：**一次调用视觉通道**，在 prompt 中说明图片顺序与关联，禁止逐张独立转写（会丢失图间关系）。
- **无关的多张图**：可分别处理；超过 2 张时优先并发调用（若平台支持）。
- 缓存按**单图**独立命中。

### 5.2 失败兜底

视觉通道出现：调用失败 / 返回空 / 返回"无法识别" / 输出不符合 schema：

1. 最多重试 1 次（换措辞或换路线 A↔B）；
2. 仍失败 → 在主上下文中显式标记 `[IMAGE UNREADABLE: <路径>]`；在回复开头标 `[转写失败]`；
3. **禁止**主模型在标记后编造图片内容；明确告知用户转写失败并给出下一步建议（换通道 / 切多模态主模型 / 手动提供文字描述）。

### 5.3 隐私提示

首过描述返回后，检查 `transcription` 是否含疑似凭据：

- 命中模式（`token=...` / `api_key:` / `password:` / `secret` / `Authorization: Bearer` / `AKID...` 等）→ 在对话内**提醒一次**用户该截图可能含敏感信息，并建议打码。
- 路线 B（外部 API）场景下提醒升级为**警告**：图片已出本机，发送到了用户配置的第三方 API。

---

## 6. 文件引用

| 文件 | 作用 |
|---|---|
| `references/schemas.md` | 完整结构化 schema、各分型细则、路线 A/B 的 prompt 模板、追问规则 |
| `scripts/vision.js` | 路线 B 的外部视觉 API 调用脚本（Node ≥ 18，零第三方依赖） |
| `assets/canary.png` | 金丝雀验证图：供用户人工裁决端点是否有原生视觉（见 §0），内容故意不在文档中描述 |

子文档细则优先于本文档；若两者冲突以 `references/schemas.md` 为准（schema 是核心资产）。

---

## 7. 平台验证记录

> 本节记录路线 A 在不同平台的实际验证结果，供未来会话引用与重新决策。

**2026-09-08 / WorkBuddy（实施方会话环境，非目标部署环境）**：

- **测试方法**：生成两张已知精确内容的测试图（错误截图 / 登录页 UI mockup），spawn general-purpose 子代理用 Read 工具读图，路由 A prompt 模板按 `references/schemas.md` §4 装配。
- **结果**：
  - 错误截图：transcription 逐字到截断行，root cause 定位到 `/home/dev/app/main.py:23` 正确，对截断位置主动写入 uncertainties。
  - UI mockup：组件层级树完整、颜色 hex 近似准确（`≈#3B82F6` 蓝）、字号层级相对描述合理、框架识别（Tailwind 风格）在 uncertainties 中显式说明"未能确定具体 UI 框架"。
- **结论**：路线 A 在 WorkBuddy 上可行。
- **⚠️ 适用范围限定**：该记录由实施方在其 WorkBuddy 会话中自报，**不迁移到 Claude Agent 双 key 部署形态**——该形态下子代理继承单模态 coding 模型，路线 A 不可用（见 §4）。WorkBuddy 宿主中主模型多有原生视觉，§0 守卫会使本 skill 休眠，均属预期行为。

**Claude Agent 框架（目标部署环境）**：

- 路线 A：默认不可用（子代理继承单模态 coding 模型）。仅聚合网关 + 子代理 model 覆盖例外可启用，启用前需在此补验证记录。
- 路线 B：默认通道。依赖 `VISION_API_KEY`，端到端自测待用户在真实环境完成（见 §8）。

**2026-09-08 / 路线 B 首跑合规性事件**：

- **现象**：状态行 `[已转写]` 出现但缺持久化文件路径；输出为散文而非 schema；全盘无 `vision-transcripts/` 目录生成——§3.1/§3.2 被跳过，且无法自证真实调用了视觉通道（是否幻觉待定性）。
- **处置**：§2.4 增加硬性反幻觉规则（未实际调用禁止输出 `[已转写]`）；新增 §3.3 审计输出块（通道/命令/退出码 + 原始返回逐字引用），作为用户核验通道真实性的强制接口；§3.1 明确状态行必须附持久化文件路径。

**2026-09-08 / LongCat 2.0 幻觉事件（守卫设计缺陷实证）**：

- **现象**：LongCat 2.0（纯文本端点）在守卫的"Read 探测 + 模型自评"机制下声称"原生视觉可用"，并把一张 KPL 电竞海报（广州TTG战队合影）完整编造为"VS Code Diff 编辑器"，细节具体到表格、代码结构、变更性质，全部为幻觉。
- **根因**：harness 接受图片且不报错时，纯文本模型无法区分"看到图"与"没看到图"，自评必然不可靠。
- **处置**：§0 守卫重写为**用户声明制**（`VISION_NATIVE_VISION=1`）+ 金丝雀图人工验证（`assets/canary.png`）；删除一切模型自评路径。该事件同时实证了状态标注机制的价值（`[未转写]` 行使异常立刻可见）。

---

## 8. 自测与未完成项

按规格 §7 的验收用例，已完成：

| # | 用例 | 状态 | 备注 |
|---|---|---|---|
| 1 | Python 报错截图转写 | ✅ | 见 §7 验证记录 |
| 2 | 登录页设计稿 → 组件层级树 | ✅ | 见 §7 验证记录 |
| 7 | 自动触发（description 匹配） | ✅ | 依赖平台 skill 加载机制 |
| 8 | 状态标注 | ✅ | 见 §3.1 4 种变体 |
| 9 | 持久化与缓存命中 | ⏳ | 设计就绪，待用户在真实使用中验证 |

依赖外部资源、未在当前会话完成的：

- 路线 B（`scripts/vision.js`）需要 `VISION_API_KEY` 才能跑通端到端；当前环境未配置，请用户用真实 key 自测。
- 错误截图 / 架构图 / 数据图 / 追问循环 / 多图关联 的全部用例需要在真实使用中跑过才能确认。

---

## 9. 安装与自测指引

1. **当前位置**：`~/Desktop/vision-proxy/`（独立目录，便于审阅与跨平台分发）。
2. **自测**：
   - 路线 A：直接发任意截图给主模型，看是否被自动加载、本流程是否被自动触发、状态标注是否出现、持久化文件是否落到 `<cwd>/vision-transcripts/`。
   - 路线 B：编辑 `<cwd>/.env`（或 `export VISION_API_KEY=...`），把图片路径喂给 `node scripts/vision.js`，看 stdout 返回与退出码。
3. **最终安装位置**（自测通过后选一个）：
   - 用户级 `~/.workbuddy/skills/vision-proxy/`：所有 WorkBuddy 项目可用；
   - 项目级 `<workspace>/.workbuddy/skills/vision-proxy/`：仅当前项目可用（注意：当前工作区是会话级沙箱路径，不适合长期存放）。

**注意**：从独立目录安装到 `~/.workbuddy/skills/` 时，记得设置 `frontmatter.agent_created: true`（已设置）以便后续用 skill_manage 修改。

---

## 10. Hook 强制转写（已部署，2026-09-08）

`hooks/vision-hook.js` 已注册为 Claude Code `UserPromptSubmit` hook（`~/.claude/settings.json`，timeout 120s）：消息到达模型**之前**由框架强制执行转写并注入上下文，模型无法绕过——这是对"弱模型模拟流程而非执行流程"的基建级兜底。

- **覆盖**：消息文本中的图片路径（拖拽/手输，含转义空格）、图片 URL、`Saved attachments:` 列表、**剪贴板粘贴图**（`[Image #N]`——从 `transcript_path` 会话记录中提取 base64 图像块，解码到 `vision-transcripts/tmp/` 后走正常转写流程，同样参与 sha1 缓存与追问）；命中缓存直接复用，共享 `vision-transcripts/` 存储。
- **降级**：粘贴图若无法从 transcript 还原（如载荷无 `transcript_path`、记录过旧），hook 注入指令要求模型必须走本 skill 流程；任何内部失败仅注入警告、**不阻塞**用户消息。
- **豁免**：`VISION_NATIVE_VISION=1` 时 hook 静默。
- 模板：`hooks/prompt-template.txt`（schema 压缩版，与 `references/schemas.md` 对齐，改动需同步）。
